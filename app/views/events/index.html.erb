<h1>Events</h1>
<div class="all_events">
<% @events.each do |event| %>
  <ul>
    <li><%= link_to event.title, event %></li>
    <li><%= event.description %></li>
    <li><%= event.date %></li>
  </ul>
<% end %>
</div>
